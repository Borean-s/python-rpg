from heroes import Hero

class Scout(Hero):

  def __init__(self, name):
    super().__init__(
      name = name,
      max_hp = 70,
      damage_multiplier = 0.8,
      movement_speed = 120,

      )

  

 
